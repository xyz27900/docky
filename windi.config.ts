import { defineConfig } from 'windicss/helpers';

export default defineConfig({
  theme: {
    extend: {
      boxShadow: {
        lgt: '0 -10px 15px -3px rgba(0, 0, 0, 0.1), 0 -4px 6px -2px rgba(0, 0, 0, 0.05)',
        lgb: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
      },
    },
  },
});
